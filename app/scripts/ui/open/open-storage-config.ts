import { h, FunctionComponent } from 'preact';
import { OpenStorageConfigView } from 'views/open/open-storage-config-view';

export const OpenStorageConfig: FunctionComponent = () => {
    return h(OpenStorageConfigView, {});
};
